# CLAUDE.md — SmartCargo ЦПП

## Что делать
Прочитай файл `IMPLEMENTATION.md` — там полная спецификация + полный исходный код рабочего прототипа (683 строки React JSX). Твоя задача: воссоздать этот прототип 1-в-1 как React+TypeScript приложение, разбив монолит на компоненты.

## Первый шаг
```
Прочитай IMPLEMENTATION.md полностью. Создай React+TypeScript+Vite проект и воссоздай весь UI и логику из прототипа. Не придумывай свой дизайн — используй ТОЧНО тот что в файле.
```

## Стек
- React 18 + TypeScript + Vite
- Tailwind CSS (или inline styles как в прототипе)
- Шрифт: DM Sans (Google Fonts)
- Mobile-first: max-width 420px

## Правила
- Язык интерфейса: русский
- Таймлайн: снизу вверх (первый этап внизу)
- Файлы именовать латиницей
- Цвета, отступы, радиусы — строго из прототипа
