/**
 * Мок-пользователь приложения. В боевой версии Smart Cargo здесь будут
 * данные авторизованного аккаунта (ИИН/БИН и телефон фиксируются при
 * регистрации). Сейчас — захардкожено, но во всех экранах потребляется
 * через единый импорт CURRENT_USER, поэтому переключение на реальную
 * авторизацию сведётся к замене этой константы на чтение из контекста.
 *
 * ВАЖНО про раздел «Мои МЛ»: серверный прокси /api/ml/uved/my-route-sheets
 * НЕ доверяет тому, что фронт передал в query. Он берёт ИИН/телефон из
 * своего собственного мока (env MOCK_UVED_IIN / MOCK_UVED_PHONE). Здесь
 * держим то же значение только чтобы UI показывал корректный профиль.
 * Значение по умолчанию — реальный тестовый ИИН Asia Transit Flow, у
 * которого на тест-стенде Smart ML есть маршрутные листы.
 */
export interface CurrentUser {
  iinBin: string;
  phone: string;
}

export const CURRENT_USER: CurrentUser = {
  iinBin: "020921500360",
  phone: "+77001234567",
};
